{
  "name": "@workspace/inwora-backend",
  "version": "0.0.0",
  "private": true,
  "description": "PHP + MySQL backend and admin panel for the INWORA portfolio.",
  "scripts": {
    "start": "C:/xampp/php/php.exe -S localhost:8000 -t . router.php",
    "setup": "C:/xampp/php/php.exe database/install.php",
    "lint": "find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l"
  }
}
